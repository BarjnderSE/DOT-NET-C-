@{
    ViewData["Title"] = "Home Page";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>BlogMe - Flipping Image Demo</title>
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <!-- Font Awesome icons (free version)-->
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
    <!-- Google fonts-->
    <link href="https://fonts.googleapis.com/css?family=Lora:400,700,400italic,700italic" rel="stylesheet" type="text/css" />
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,800italic,400,300,600,700,800" rel="stylesheet" type="text/css" />
    <!-- Core theme CSS (includes Bootstrap)-->
    <link href="css/styles.css" rel="stylesheet" />

    <!-- Custom CSS for Flipping Effect -->
    <style>
        /* Basic body styling */
        body {
            font-family: 'Open Sans', sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
            color: #333;
        }

        /* Header styling */
        .masthead {
            background-image: url('assets/img/home-bg.jpg');
            background-size: cover;
            background-position: center;
            color: white;
            text-shadow: 2px 2px 5px rgba(0,0,0,0.7);
            padding: 120px 0;
            text-align: center;
        }

            .masthead h1 {
                font-family: 'Lora', serif;
                font-size: 3.5rem;
                font-weight: bold;
                margin-bottom: 20px;
            }

            .masthead .subheading {
                font-size: 1.25rem;
                font-style: italic;
                color: #ddd;
            }

        /* Container for Flipping Image */
        .flip-container {
            perspective: 1000px;
            margin-top: 50px;
        }

        .flipper {
            position: relative;
            width: 100%;
            max-width: 400px;
            height: 300px;
            margin: 0 auto;
            transition: transform 0.6s;
            transform-style: preserve-3d;
        }

        .flip-container:hover .flipper {
            transform: rotateY(180deg);
        }

        .front, .back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 10px;
        }

        .front {
            background-image: url('https://via.placeholder.com/400x300');
            background-size: cover;
            background-position: center;
            border: 5px solid #fff;
        }

        .back {
            background-color: #f39c12;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 1.25rem;
            text-transform: uppercase;
            font-weight: bold;
            border-radius: 10px;
            transform: rotateY(180deg);
            padding: 20px;
        }

        .text-center {
            text-align: center;
        }

        h2 {
            color: #333;
            font-size: 2.5rem;
            margin-bottom: 30px;
        }

        /* Buttons and links styling */
        .btn-primary, .btn-warning {
            background-color: #f39c12;
            border: none;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
        }

            .btn-primary:hover, .btn-warning:hover {
                background-color: #e67e22;
            }

        .btn-secondary {
            background-color: #333;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 1rem;
            transition: all 0.3s;
        }

            .btn-secondary:hover {
                background-color: #555;
            }
    </style>
</head>
<body>
    <!-- Page Header-->
    <header class="masthead">
        <div class="container position-relative px-4 px-lg-5">
            <div class="row gx-4 gx-lg-5 justify-content-center">
                <div class="col-md-10 col-lg-8 col-xl-7">
                    <div class="site-heading">
                        <h1>Welcome to My Blog</h1>
                        <span class="subheading">Explore, Learn & Grow</span>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Flipping Image Content-->
    <div class="container px-4 px-lg-5 my-5 text-center">
        <h2>Featured Topic: HTML & CSS</h2>
        <div class="flip-container">
            <div class="flipper">
                <div class="front"></div>
                <div class="back">
                    <p>Learn HTML & CSS to Build Stunning Websites</p>
                </div>
            </div>
        </div>
        <div class="mt-4">
            <a href="#more-info" class="btn btn-primary">Learn More</a>
            <a href="#subscribe" class="btn btn-warning">Subscribe Now</a>
        </div>
    </div>

    <!-- Subscribe Section (example)-->
    <div id="subscribe" class="container text-center my-5">
        <h3>Stay Updated with Our Latest Posts</h3>
        <form class="mt-4">
            <input type="email" class="form-control mb-3" placeholder="Enter your email" style="max-width: 400px; margin: 0 auto;" />
            <button type="submit" class="btn btn-secondary">Subscribe</button>
        </form>
    </div>

    <!-- Bootstrap core JS-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Core theme JS-->
    <script src="js/scripts.js"></script>
</body>
</html>
